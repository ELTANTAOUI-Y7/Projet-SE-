global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus self-monitoring
  
 # - job_name: 'prometheus'
  #  static_configs:
   #   - targets: ['localhost:9090']

  # Phone Shop Application
  #- job_name: 'phone-shop-app'
   # static_configs:
    #  - targets: ['phone-shop:8080']
    #metrics_path: /metrics
    #scrape_interval: 30s

  # Jenkins
  - job_name: 'jenkins'
    static_configs:
      - targets: ['127.0.0.1:8090']  # Access host Jenkins from Docker
    metrics_path: /prometheus
    scrape_interval: 30s
